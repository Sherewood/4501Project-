#Use for edeniteDevil
#special ability
If unitLowHealth and specialAbilityReady then activateSpecialAbility
#basically - focus on whichever target picked up, and if there is no target, move towards the main base
If init then requestMainBasePos
If positionReceived then moveToDestination
If targetChanged and targetNotInRange then setFocusTarget and moveTarget
If targetChanged and targetInRange then setFocusTarget and attackTarget
If targetLost then clearTarget and stopMovement and requestMainBasePos
If targetInRange then attackTarget
If targetNotInRange then moveTarget